// @flow

export default class {{pascalCase name}} extends MotherBoard {

static #instance: {{pascalCase name}};

  constructor() {
    if ({{pascalCase name}}.#instance) {
      throw new Error('Use {{pascalCase name}}.getInstance()');
    }
    super();
    {{pascalCase name}}.#instance = this;
  }

  static getInstance(): {{pascalCase name}} {
    if ({{pascalCase name}}.#instance) {
      return {{pascalCase name}}.#instance;
    }
    return new {{pascalCase name}}();
  }

  init(): void {
    super.init();

    // your componentsMap can be set in this place!
    this.componentsMap = {};
  }

  bind(): void {
    super.bind();
  }

  onload(): void {
    super.onload();
  }

  destroy(): void {
    super.destroy();
  }
}
